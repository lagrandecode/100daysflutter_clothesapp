

import 'package:flutter/cupertino.dart';

class ShowsDisplay extends StatelessWidget {
  const ShowsDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("hello"),
    );
  }
}
